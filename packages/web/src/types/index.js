// Local types to avoid shared package import issues

export const UserRole = {
  STUDENT: 'student',
  TEACHER: 'teacher',
  PARENT: 'parent',
  ADMIN: 'admin',
  SUPER_ADMIN: 'super_admin',
};

// JavaScript doesn't need interface definitions
// These were TypeScript interfaces that are no longer needed
